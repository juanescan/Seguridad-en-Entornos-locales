version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_PASSWORD=admin123
      - API_KEY=sk_test_local_development
    volumes:
      - ./src:/app/src
    command: java -jar app.jar
